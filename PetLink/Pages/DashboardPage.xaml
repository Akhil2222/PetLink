<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PetLink.Pages.DashboardPage"
             Title="DashboardPage">
    <VerticalStackLayout>
        <Label Text="{Binding Title}"
           FontSize="Large" 
           TextColor="Blue" 
           FontAttributes="Bold"
           Margin="80,50,0,0"/>

        <Grid Padding="16,24,16,16"
              RowSpacing="6"
              ColumnSpacing="12">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Row="0"
                    Grid.Column="1"
                    Text="{Binding Profile}"
                    Command="{Binding ProfileClickedCommand}"
                    Style="{StaticResource TileButton}"
                    WidthRequest="300"/>

            <Button Grid.Row="1"
                    Grid.Column="1"
                    Text="{Binding FindMyPet}"
                    Command="{Binding FindMyPetClickedCommand}"
                    Style="{StaticResource TileButton}"
                    WidthRequest="300"/>
            
            <Button Grid.Row="2"
                    Grid.Column="1"
                    Text="{Binding LikedPets}"
                    Command="{Binding LikedPetsClickedCommand}"
                    Style="{StaticResource TileButton}"
                    WidthRequest="300"/>
        </Grid>

    </VerticalStackLayout>
</ContentPage>